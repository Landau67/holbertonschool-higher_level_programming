#!/usr/bin/python3
import sys
if len(sys.argv) > 1:
	l = len(sys.argv) - 1
	if l == 1:
        	print("1 argument:")
	else:
        	print("{} arguments:".format(l))
	for i in range (1, l + 1):
		print("{}: {}".format(i, sys.argv[i]))
else:
	print("0 arguments.")
